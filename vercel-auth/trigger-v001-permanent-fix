MH Analysis V.01 COMPLETE corrected baseline rebuild retry. CI now verifies actual generated behavior instead of a comment string. Preserve all user/app/trading data; require username + password every fresh EXE launch; restore MH Analysis, WhatsApp/Signal Link, Records, MT5, KEYS, EXIT and Get Signal after login. Current V.01 has silent update check with no update UI. Future V.02+ mandatory update appears before login, installs replacement without deleting data, then returns to Account Login. One outer window frame only. Installer MH-Analysis.exe.
