MH Analysis V.01 full contract rebuild. Fresh-login enforcement is now independent of legacy auth bootstrap formatting. Keep version exactly V.01. Preserve all saved user/app/trading data across close/reopen/reinstall/update, but require username + password on every fresh EXE process. After login restore MH Analysis, WhatsApp/Signal Link, Records, MT5, KEYS, EXIT and Get Signal. Current V.01 silently checks backend and shows no update UI when current. Future verified V.02+ must show mandatory update before login, download/install replacement with progress without deleting saved data, then reopen at Account Login. Exactly one outer Minimize/Maximize/Close frame; no inner frame/update scrollbar. Installer filename MH-Analysis.exe.
