MH Analysis V.01 corrected baseline rebuild: fresh-login patch now handles the actual V807 verify-first auth bootstrap. Preserve saved user/app/trading data; require username + password on every fresh EXE process; after login restore MH Analysis, WhatsApp/Signal Link, Records, MT5, KEYS, EXIT and Get Signal. Current V.01 silently checks updates without update UI. Future V.02+ mandatory update appears before login, installs without deleting data, then starts at Account Login. One outer native frame only. Installer MH-Analysis.exe.
