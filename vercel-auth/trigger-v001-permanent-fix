MH Analysis V.02 authoritative runtime build #37. Follow this contract exactly on every future update: preserve device binding and all non-auth user/trading/browser data; NEVER preserve login authorization across EXE processes; username/password mandatory after every close/reopen and after every update; mandatory newer-version screen before login; current version update check silent; only one outer MH Analysis Minimize/Maximize/Close frame; embedded Analysis/WhatsApp/Records/MT5 surfaces have no own frame; after manual login KEYS, EXIT, Get Signal, MH Analysis, WhatsApp + Signal Link, Records and MT5 must remain visible and functional. The authoritative runtime patch is applied LAST so older patches cannot overwrite these rules. Installer filename exactly MH-Analysis.exe.