MH Analysis V.01 COMPLETE corrected baseline rebuild. One contract only: preserve all user/app/trading data across close, reopen, reinstall and update; require username + password on every fresh EXE launch; after successful login restore and keep working MH Analysis, WhatsApp/Signal Link, Records, MT5, KEYS, EXIT and Get Signal. Current V.01 silently checks backend and never shows update UI when current. Future verified V.02+ appears before login, blocks access, shows update/download/install progress, replaces app without deleting saved data, then updated EXE starts at Account Login again. Keep exactly one outer Minimize/Maximize/Close frame and no update scrollbar. Installer filename MH-Analysis.exe.
