MH Analysis V.01 FINAL contract rebuild. Preserve all existing user/app/trading data across reinstall and update, but never preserve login authorization across EXE close/reopen: every fresh launch requires username and password. After successful login restore MH Analysis, WhatsApp, Records and MT5 exactly as normal and keep their actions working. Current V.01 must silently pre-check updates and show no update screen when current. Future verified V.02+ must appear before login; Update Now shows download/install progress, replaces the app while preserving data, then updated app requires Account Login again. Keep one outer Min/Max/Close frame only. Installer filename MH-Analysis.exe.
