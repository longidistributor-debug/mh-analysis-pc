const fs=require('fs');
function read(p){return fs.readFileSync(p,'utf8').replace(/\r\n/g,'\n')}
function write(p,s){fs.writeFileSync(p,s,'utf8')}

const p='analysis_context_v569.go';
let s=read(p);
const a=s.indexOf('func v569AnalysisSnapshotHandler(w http.ResponseWriter, r *http.Request) {');
const b=s.indexOf('\nfunc v569Outcome(',a);
if(a<0||b<0)throw new Error('v569AnalysisSnapshotHandler block not found');

const replacement=`// V56.12: use the three FCS endpoints directly instead of multi_url.\n// One analysis action = exactly three FCS provider calls:\n// 1) selected timeframe history, 2) true HTF history, 3) latest bid/ask.\n// No fallback provider call is made, so this cannot silently exceed three calls per analysis.\ntype v5612DirectPart struct {\n\tIndex int\n\tRoot any\n\tStatus int\n\tErr error\n}\n\nfunc v5612FetchDirectFCS(client *http.Client, endpointPath, apiKey string) (any, int, error) {\n\tu, err := url.Parse("https://api-v4.fcsapi.com/" + strings.TrimPrefix(strings.TrimSpace(endpointPath), "/"))\n\tif err != nil { return nil, 0, err }\n\tz := u.Query()\n\tz.Set("access_key", apiKey)\n\tu.RawQuery = z.Encode()\n\treq, err := http.NewRequest(http.MethodGet, u.String(), nil)\n\tif err != nil { return nil, 0, err }\n\treq.Header.Set("User-Agent", "MH-Analysis/V56.12")\n\tresp, err := client.Do(req)\n\tif err != nil { return nil, 0, err }\n\tdefer resp.Body.Close()\n\tbody, err := io.ReadAll(io.LimitReader(resp.Body, 16<<20))\n\tif err != nil { return nil, resp.StatusCode, err }\n\tvar root any\n\tif json.Unmarshal(body, &root) != nil {\n\t\treturn nil, resp.StatusCode, fmt.Errorf("provider returned unreadable JSON")\n\t}\n\treturn root, resp.StatusCode, nil\n}\n\nfunc v569AnalysisSnapshotHandler(w http.ResponseWriter, r *http.Request) {\n\tw.Header().Set("Content-Type", "application/json")\n\tif r.Method != http.MethodPost {\n\t\thttp.Error(w, "method", http.StatusMethodNotAllowed)\n\t\treturn\n\t}\n\tif !allowCall() {\n\t\tw.WriteHeader(http.StatusTooManyRequests)\n\t\t_ = json.NewEncoder(w).Encode(map[string]any{"error": "Too many fresh market requests. Please wait about a minute."})\n\t\treturn\n\t}\n\tvar q v569SnapshotRequest\n\tif json.NewDecoder(r.Body).Decode(&q) != nil {\n\t\thttp.Error(w, "bad request", http.StatusBadRequest)\n\t\treturn\n\t}\n\tcfg := getSettings()\n\tif strings.TrimSpace(cfg.APIKey) == "" {\n\t\tw.WriteHeader(http.StatusUnauthorized)\n\t\t_ = json.NewEncoder(w).Encode(map[string]any{"error": "Access Key is not saved."})\n\t\treturn\n\t}\n\tprimaryPeriod := normalizePeriod(q.Period)\n\thtfPeriod := v569HigherPeriod(primaryPeriod)\n\tp1, p2, p3, supported := v569MarketPaths(q.Symbol, primaryPeriod, htfPeriod)\n\tif !supported {\n\t\tw.WriteHeader(http.StatusBadRequest)\n\t\t_ = json.NewEncoder(w).Encode(map[string]any{"error": "Unsupported symbol"})\n\t\treturn\n\t}\n\n\tclient := &http.Client{Timeout: 20 * time.Second}\n\tpaths := []string{p1,p2,p3}\n\tch := make(chan v5612DirectPart, 3)\n\tfor i, path := range paths {\n\t\tgo func(idx int, endpoint string) {\n\t\t\troot, status, err := v5612FetchDirectFCS(client, endpoint, cfg.APIKey)\n\t\t\tch <- v5612DirectPart{Index:idx, Root:root, Status:status, Err:err}\n\t\t}(i, path)\n\t}\n\tparts := make([]v5612DirectPart, 3)\n\tfor i:=0;i<3;i++ {\n\t\tx := <-ch\n\t\tparts[x.Index] = x\n\t}\n\n\tfor i, part := range parts {\n\t\tif part.Err != nil {\n\t\t\tw.WriteHeader(http.StatusBadGateway)\n\t\t\t_ = json.NewEncoder(w).Encode(map[string]any{"error": fmt.Sprintf("FCS request %d failed: %v", i+1, part.Err)})\n\t\t\treturn\n\t\t}\n\t\tif part.Status >= 400 {\n\t\t\tw.WriteHeader(part.Status)\n\t\t\t_ = json.NewEncoder(w).Encode(map[string]any{"error": v569DeepError(part.Root, fmt.Sprintf("FCS request %d failed", i+1))})\n\t\t\treturn\n\t\t}\n\t}\n\n\tprimary := extractCandles(parts[0].Root)\n\thtf := extractCandles(parts[1].Root)\n\tif len(primary) > 300 { primary = primary[len(primary)-300:] }\n\tif len(htf) > 300 { htf = htf[len(htf)-300:] }\n\tif len(primary) == 0 {\n\t\tw.WriteHeader(http.StatusBadGateway)\n\t\t_ = json.NewEncoder(w).Encode(map[string]any{\n\t\t\t"error": v569DeepError(parts[0].Root, "FCS selected-timeframe history returned no usable candles"),\n\t\t\t"primary_shape": fmt.Sprintf("%T", parts[0].Root),\n\t\t})\n\t\treturn\n\t}\n\n\task, bid, closePrice, quoteOK := v569FindQuote(parts[2].Root)\n\tquote := map[string]any{"available": quoteOK}\n\tif quoteOK {\n\t\tspread := math.Max(0, ask-bid)\n\t\tquote["ask"] = ask\n\t\tquote["bid"] = bid\n\t\tquote["close"] = closePrice\n\t\tquote["spread"] = spread\n\t\tif closePrice > 0 { quote["spread_pct"] = spread / closePrice * 100 }\n\t}\n\n\t_ = json.NewEncoder(w).Encode(map[string]any{\n\t\t"candles": primary,\n\t\t"htf_candles": htf,\n\t\t"htf_period": v569DisplayPeriod(htfPeriod),\n\t\t"quote": quote,\n\t\t"meta": map[string]any{\n\t\t\t"source": "FCS direct precision snapshot",\n\t\t\t"symbol": strings.ToUpper(strings.TrimSpace(q.Symbol)),\n\t\t\t"period": q.Period,\n\t\t\t"count": len(primary),\n\t\t\t"htf_count": len(htf),\n\t\t\t"external_api_calls": 3,\n\t\t\t"datasets": 3,\n\t\t},\n\t})\n}\n`;

s=s.slice(0,a)+replacement+s.slice(b);
write(p,s);
console.log('V56.12 direct 3-call market snapshot applied');
